name: Build codewhale (riscv64)
on: [push, pull_request]
jobs:
  call:
    uses: ./.github/workflows/build-package.yml
    with:
      package: "codewhale"
      runs-on: "ubuntu-latest"
      riscv64-cross: true
    secrets: inherit
