name: Build ruyi (x86_64)
on: [push, pull_request]
jobs:
  call:
    uses: ./.github/workflows/build-package.yml
    with:
      package: "ruyi"
      runs-on: "ubuntu-latest"
      riscv64-cross: false
    secrets: inherit
