name: Build opencode-telegram (x86_64)
on: [push, pull_request]
jobs:
  call:
    uses: ./.github/workflows/build-package.yml
    with:
      package: "opencode-telegram"
      runs-on: "ubuntu-latest"
      riscv64-cross: false
    secrets: inherit
