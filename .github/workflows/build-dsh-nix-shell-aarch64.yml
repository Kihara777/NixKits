name: Build dsh-nix-shell (aarch64)
on: [push, pull_request]
jobs:
  call:
    uses: ./.github/workflows/build-package.yml
    with:
      package: "dsh-nix-shell"
      runs-on: "ubuntu-24.04-arm"
      riscv64-cross: false
    secrets: inherit
