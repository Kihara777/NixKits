name: Build dsh-preset-news-three-elements (x86_64)
on: [push, pull_request]
jobs:
  call:
    uses: ./.github/workflows/build-package.yml
    with:
      package: "dsh-preset-news-three-elements"
      runs-on: "ubuntu-latest"
      riscv64-cross: false
    secrets: inherit
